[project]
name = "hello-world-mcp-server"
version = "0.1.0"
description = "A simple HTTP-stream MCP server using FastMCP"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=0.3.0",
]

[project.scripts]
server = "server:main"
